import React, { useEffect } from "react";

const Countries = () => {
	// le useEffect se jouen tlorsque le composant est monté
	useEffect(() => {}, []);

	return (
		<div className="countries">
			<h1>COUNTRIES</h1>
		</div>
	);
};

export default Countries;
